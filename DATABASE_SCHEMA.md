# AI Documents — Database Schema

All data is stored inside the standard WordPress database. No custom tables are created.

---

## 1. Custom Post Type — `aidoc`

**Table:** `wp_posts`

| Column | Value |
|---|---|
| `post_type` | `aidoc` |
| `post_title` | Document title |
| `post_status` | `publish` / `draft` / etc. |
| `post_date` | WordPress creation date (auto) |

---

## 2. Post Meta — `wp_postmeta`

One row per field per document (`post_id` = ID of the `aidoc` post).

| `meta_key` | Type | Description |
|---|---|---|
| `_document_file_id` | `int` | WordPress attachment ID of the uploaded file |
| `_document_pub_date` | `string` | Publication date in `YYYY-MM-DD` format |
| `_document_file_format` | `string` | One of: `pdf`, `word`, `excel` |
| `_document_description` | `text` | Value of the built-in Description field |
| `_document_content` | `text` (JSON) | Structured document body as a JSON array of blocks, produced by regex parsing of the PDF text (no AI). Written by the "Extract Content" button. See *Content block shape* below. |
| `_document_history` | `text` | Provenance line from the source document's `Document History` section (approval and revision dates). Written by "Extract Content" when the document uses the labelled schema. |
| `_document_summary` | `text` | 1–2 sentence AI-generated summary (written by "Process with AI" or "Index for Search"). Used to enrich semantic indexing. |
| `_document_embedding` | `text` (JSON) | 768-dimension float vector as a JSON array (e.g. `[0.012, -0.034, …]`). Generated by the Gemini `text-embedding-004` model. Used for cosine-similarity search. |

### Content block shape

`_document_content` holds a JSON array. Every element is exactly one of:

```json
{ "type": "heading",   "level": 2, "text": "REDUCED-CREDIT UNDERGRADUATE DEGREES" }
{ "type": "paragraph", "text": "As part of its review of an institution…" }
{ "type": "list",      "ordered": false, "items": ["The name of the program must…"] }
```

Deliberately minimal: `aidocs_parse_structured_content()` is the only writer and `aidocs_render_content_blocks()` the only reader, so the parser can be retuned per document family without touching the renderer, the modal, or the single-document view. `aidocs_content_plain_text()` flattens the blocks back to text for search indexing and AI context.

### Authored label schema

The source policies are written in Word against a fixed set of literal text labels. `aidocs_parse_labeled_document()` matches these, which is what makes extraction deterministic rather than heuristic:

```
<Document title>
Teaser: <one-paragraph summary>          → _document_description
Body:                                    → _document_content (parsed into blocks)
<paragraphs and bulleted requirements>
Last Updated: <Month YYYY> (<body>)      → _document_pub_date
Document History: <provenance>           → _document_history
```

Each marker is accepted in three interchangeable spellings — `Label:`, `[Label]`, and a line containing nothing but the label — because all three occur in the authored files. A delimiter is always required, so prose that merely begins with a label word is not mistaken for a section boundary. Provenance lines that follow `Last Updated:` without their own marker are folded into the history.

Crucially, these are **text** labels, so they survive PDF export and text extraction. The light-blue heading colour (`4F81BD`) that separates documents in the Word master is a *style* property and is lost once text is pulled out of a PDF — which is why the labels, not the colour, are the extraction contract.

Documents that lack `Teaser:` and `Body:` fall back to `aidocs_parse_structured_content()`'s capitalisation and bullet heuristics.

### Writing JSON meta — always `wp_slash()`

`update_post_meta()` runs `wp_unslash()` on the value it is given (WordPress's "expects slashed data" convention). Passing raw `wp_json_encode()` output therefore strips the backslashes out of JSON's `\uXXXX` escapes, and the stored meta no longer parses:

```php
update_post_meta( $id, '_document_content', wp_json_encode( $blocks ) );              // corrupts
update_post_meta( $id, '_document_content', wp_slash( wp_json_encode( $blocks ) ) );  // correct
```

This fails **silently and only on non-ASCII content** — a document of plain ASCII round-trips fine, while one containing curly quotes or em dashes stores `u201c` instead of `“` and reads back as an empty block list. Both `_document_content` and `_document_embedding` writes are wrapped accordingly.

### Notes
- `_document_embedding` can be large (~18 KB per document). For catalogs under ~500 documents this is acceptable in `wp_postmeta`.
- `_document_summary` and `_document_embedding` are written automatically when "Process with AI" or "Index for Search" is used. They are never written on manual save.

---

## 3. Taxonomies

### `document_audience`
**Tables:** `wp_terms`, `wp_term_taxonomy`, `wp_term_relationships`

Stores audience labels (e.g. `Institution`, `Evaluator`, `Public`). Terms are created automatically when the taxonomy list is saved in Settings → Taxonomy.

### `document_type`
**Tables:** `wp_terms`, `wp_term_taxonomy`, `wp_term_relationships`

Stores document type labels (e.g. `Policies`, `Guidelines`, `Handbooks`). Terms are created automatically when the type list is saved in Settings → Taxonomy.

Both taxonomies are registered with `show_ui = false` — they are managed exclusively through the document editor meta box and the Settings panel, not through the default WordPress taxonomy screens.

---

## 4. Plugin Options — `wp_options`

One row per option. All keys are prefixed with `aidocs_`.

| `option_name` | Type | Default | Description |
|---|---|---|---|
| `aidocs_gemini_api_key` | `string` | *(empty)* | Google Gemini API key |
| `aidocs_gemini_model` | `string` | `gemini-2.5-flash` | Gemini model used for text generation |
| `aidocs_audiences_list` | `string` | newline-separated defaults | One audience label per line. Used by `aidocs_get_audiences()`. |
| `aidocs_types_list` | `string` | newline-separated defaults | One document type per line. Used by `aidocs_get_types()`. |
| `aidocs_legacy_migrated` | `string` | *(unset)* | Plugin version that ran the one-time rename migration (see §7). Not autoloaded. |
| `aidocs_removed_options_cleaned` | `string` | *(unset)* | Plugin version that ran the one-time cleanup of options for removed features (see §7). Not autoloaded. |

The CPT's menu label/icon, archive slug, and allowed file formats are hardcoded (`Documents`, `dashicons-media-document`, `documents`, `pdf`/`word`/`excel`) — no longer configurable via options.

---

## 5. Transients — `wp_options` (with `_transient_` prefix)

| Key | TTL | Value | Description |
|---|---|---|---|
| `aidocs_embed_model` | 1 hour | `{ "name": "models/text-embedding-004", "ver": "v1beta" }` | Cached result of the ListModels API call. Stores the first available Gemini embedding model and its API version. Avoids repeating the discovery request on every embedding call. |

---

## 6. WordPress Media Library — `wp_posts` + `wp_postmeta`

Uploaded document files are stored as standard WordPress attachments (`post_type = attachment`). The plugin stores only the attachment ID in `_document_file_id`. The actual file URL, path, and size are retrieved via standard WordPress functions (`wp_get_attachment_url`, `get_attached_file`).

---

## 7. Legacy Namespace Migration (v1.1.0)

Version 1.1.0 renamed the plugin's namespace from `cirlot_*` to `aidocs_*`. `aidocs_maybe_migrate_legacy()` runs once on `plugins_loaded` (and on activation) and applies:

| Was | Is now |
|---|---|
| `post_type = cirlot_document` | `post_type = aidoc` |
| `wp_options` rows named `cirlot_docs_*` | renamed to `aidocs_*` |
| `_transient_cirlot_docs_embed_model` | deleted (rediscovered on next embedding call) |
| `[cirlot_document_search …]` in `post_content` | rewritten to `[aidocs_search …]` |

Post meta (`_document_*`) and both taxonomies (`document_audience`, `document_type`) were never prefixed and are left untouched. After the run, `aidocs_legacy_migrated` is set and `rewrite_rules` is dropped so permalinks rebuild against the new post type.

### Removed-features cleanup (v1.2.0)

v1.2.0 removed the General settings tab and the configurable Custom Fields system (see the plugin `README.md`). `aidocs_maybe_cleanup_removed_options()` runs once on `plugins_loaded` and deletes the now-orphaned option rows: `aidocs_menu_name`, `aidocs_menu_icon`, `aidocs_archive_slug`, `aidocs_default_audience`, `aidocs_default_type`, `aidocs_allowed_formats`, `aidocs_global_fields`. Any values saved in `_document_cf_*` post meta from custom fields created before the upgrade are left in place (harmless, simply no longer read or written) rather than deleted, since deleting post meta in bulk based on a key pattern is riskier than leaving orphaned rows behind.

---

## Summary Diagram

```
wp_posts
└── post_type = aidoc
    ├── post_title        (document title)
    └── post_id → wp_postmeta
                  ├── _document_file_id         → wp_posts (attachment)
                  ├── _document_pub_date
                  ├── _document_file_format
                  ├── _document_description
                  ├── _document_content         (JSON block[])
                  ├── _document_history
                  ├── _document_summary
                  └── _document_embedding       (JSON float[768])

wp_term_relationships
└── post_id → document_audience terms (wp_terms)
└── post_id → document_type terms     (wp_terms)

wp_options
├── aidocs_*          (plugin settings)
└── _transient_aidocs_embed_model
```
